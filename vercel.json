{
  "projects": [
    {
      "src": "apps/tastebud/package.json",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "pnpm install && pnpm --filter tastebud... run build",
        "outputDirectory": "apps/tastebud/dist"
      }
    }
  ]
}
