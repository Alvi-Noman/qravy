PORT=8090

# Where your Tastebud dev server runs (from INSIDE the container):
# On Docker Desktop (Windows/macOS), use host.docker.internal:<vite-port>
# On Linux, replace with your host's IP (e.g., http://192.168.1.50:<vite-port>)
TASTEBUD_DEV_URL=http://host.docker.internal:3007

# Where your API Gateway runs (container-to-container in dev):
GATEWAY_URL=http://api-gateway:8080

# Comma-separated exact origins you allow for CORS in dev (Vite + storefront-host)
CORS_ORIGIN=http://localhost:3007,https://localhost:3007,http://localhost:8090,https://localhost:8090
