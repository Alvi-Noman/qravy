# SPA uses /api/v1 (proxied by storefront-host in dev)
VITE_API_URL=/api/v1

# Optional metadata for UI
VITE_APP_NAME=Tastebud
VITE_ENABLE_SUBDOMAIN_DEV=true

# Optional helper reference for local proxy
TASTEBUD_DEV_URL=http://host.docker.internal:3007
