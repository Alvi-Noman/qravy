import { describe, it, expect } from 'vitest';

/**
 * Placeholder suite for the Onboarding Wizard.
 * TODO: Replace with real integration tests once the wizard is finalized.
 */
describe('Onboarding Wizard (placeholder)', () => {
  it('will be implemented once onboarding is finalized', () => {
    expect(true).toBe(true);
  });

  it.skip('navigates through steps (to be implemented)', () => {
    // TODO: StepWelcome -> StepLocation -> StepTableCount -> completion
  });
});